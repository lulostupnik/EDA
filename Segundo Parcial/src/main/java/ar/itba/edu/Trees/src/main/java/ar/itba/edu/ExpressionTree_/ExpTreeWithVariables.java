package ar.itba.edu.ExpressionTree_;
// Me da fiaca hacerlo, pero es lo mismo que la clase ExpTree con las siguientes salvedades:
// - En el constructor recibe un Map<String, Double> con las variables y sus valores
// - Debo modificar el método buildExpression para que si el token es una variable, la guarde en data al igual que a una constante
// - Debo modificar el método evaluate para que si el token es una variable, busque su valor en el Map y lo devuelva

// - Podría en realidad hacer que el método buildExpression reemplace la variable por su valor en el Map en vez de guardarla en data

// puede que me esté olvidando de algo